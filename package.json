{
  "name": "comuni",
  "version": "1.0.0",
  "description": "Proyecto comuni.",
  "main": "src/server.ts",
  "scripts": {
    "start-production": "node ./lib/server.js",
    "start-dev": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec ts-node src/server.ts"
  },
  "keywords": [
    "comuni"
  ],
  "author": "Jose Zavala (Josbla-Jenkins)",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.16.0",
    "express": "^4.16.3",
    "mongoose": "^5.3.0",
    "nconf": "^0.10.0",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.1",
    "winston": "^3.1.0"
  }
}
